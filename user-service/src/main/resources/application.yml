server:
  port: 8081

spring:
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

token:
  expiration_time: 86400000
  secret: user_token
